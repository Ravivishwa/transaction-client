/*
 * ListData Messages
 *
 * This contains all the text for the ListData container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListData';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListData container!',
  },
});
